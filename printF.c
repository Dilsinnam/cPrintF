#include<stdio.h>
#include<stdlib.h>

int main () {
    int shirtNumber = 15;
    printf("Hello\nWorld\n");
    printf("My favourite %s is %d, but also is %f.", "number", shirtNumber, 3.14);/*%d to print out an integer, followed by said integer. %s is for text. %f is for a decimal.*/

    return 0;
}   